{
	"name": "m_dim_currency",
	"properties": {
		"type": "MappingDataFlow",
		"typeProperties": {
			"sources": [
				{
					"dataset": {
						"referenceName": "pre_dim_currency",
						"type": "DatasetReference"
					},
					"name": "preDimCurrency"
				}
			],
			"sinks": [
				{
					"dataset": {
						"referenceName": "dim_currency",
						"type": "DatasetReference"
					},
					"name": "dimCurrency"
				}
			],
			"transformations": [
				{
					"name": "DeriveTopLevel",
					"description": "Deriving top_key/descr."
				},
				{
					"name": "AlterRow"
				}
			],
			"script": "source(output(\n\t\tpk_key as string,\n\t\tcur_description as string,\n\t\tcurr_last_exchange_rate_to_usd as decimal(12,2),\n\t\tlast_updated_by as string,\n\t\tHashedValues as string\n\t),\n\tallowSchemaDrift: true,\n\tvalidateSchema: false,\n\tformat: 'parquet',\n\twildcardPaths:['Pre_Dim/pre_dim_currency.parquet']) ~> preDimCurrency\npreDimCurrency derive(top_code = 'ALL',\n\t\ttop_description = 'All Currencies') ~> DeriveTopLevel\nDeriveTopLevel alterRow(upsertIf(!isNull(top_code))) ~> AlterRow\nAlterRow sink(allowSchemaDrift: true,\n\tvalidateSchema: false,\n\tformat: 'parquet',\n\tpartitionFileNames:['dim_currency.parquet'],\n\tpartitionBy('hash', 1),\n\tskipDuplicateMapInputs: true,\n\tskipDuplicateMapOutputs: true) ~> dimCurrency"
		}
	}
}